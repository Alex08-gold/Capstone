package letsdothis.src;

import letsdothis.src.InputWindow;
import letsdothis.src.Terrain;

public class UserInteraction {
	public static void main(String[] args) {
		
		// Set up first GUI to retrieve user input
		int defaultTerrainHeight = 500;
		int defaultTerrainWidth = 500;
//		Terrain terrain = new Terrain(defaultTerrainHeight, defaultTerrainWidth);
//		InputWindow firstWindow = new InputWindow(terrain);
//		firstWindow.setVisible(true);
	}
}